import React from 'react';
import ContentAbout from '../../container/about';
import { Layout } from '../../Layout';

const AboutPage = () => {
    return (
        <Layout>
            <ContentAbout />
        </Layout>
    );
};

export default AboutPage;